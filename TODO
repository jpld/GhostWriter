
[X] add sample composition
[X] save as png to Destination File Path input
[ ] sort out if the plug-in supports 10.5 or not
[ ] have a setting that auto-appends a marker to the Destination File Path input - default yes
[ ] change Destination File Path to something else, dirname + basename + extname
[ ] have a setting to pick image format PNG vs JPEG
