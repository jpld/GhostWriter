
[X] add sample composition
[X] save as png to Destination File Path input
[X] sort out if the plug-in supports 10.5 or not - NO due to use of NS_RETURNS_RETAINED and CGColorSpaceCopyName())
[ ] dup patch to offer a more advanced version with more control over output file name
[ ] some flavor of testing would be nice

BASIC
[ ] if replacement marker is present in File Destination Path, replace with auto-incrementing value

ADVNACED
[ ] add dirname and basename inputs
[ ] popup setting to select image format
[ ] toggle setting to append auto-incrementing marker to basename, divine starting number by scanning dirname
[ ] toggle input to allow capture from GL world rather than explicit image input
[ ] sample composition
